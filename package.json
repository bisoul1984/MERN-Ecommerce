{
  "name": "mern-ecommerce",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && DISABLE_ESLINT_PLUGIN=true CI=false npm run build",
    "start": "cd frontend && npm start"
  }
} 